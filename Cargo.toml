[package]
name = "soroban-debug-starter-plugin"
version = "0.1.0"
edition = "2021"
description = "A starter template for Soroban Debugger plugins"

[lib]
crate-type = ["cdylib"]

[dependencies]
soroban-debugger = { path = "../../../" }
tracing = "0.1"