[package]
name = "example_logger_plugin"
version = "1.0.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
soroban-debugger = { path = "../../.." }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

