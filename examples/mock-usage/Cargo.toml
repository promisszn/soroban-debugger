[package]
name = "mock-usage"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
soroban-sdk = "22.0.0"

[dev-dependencies]
soroban-debug-mock = { path = "../../crates/soroban-debug-mock" }
serde_json = "1.0"
